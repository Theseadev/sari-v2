"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.Semaphore = exports.MAX_PARALLEL_CONCURRENCY = exports.DEFAULT_GLOBAL_CONCURRENCY_LIMIT = void 0;exports.aggregateParallelOutputs = aggregateParallelOutputs;exports.flattenSteps = flattenSteps;exports.isDynamicRunnerGroup = isDynamicRunnerGroup;exports.isParallelGroup = isParallelGroup;exports.mapConcurrent = mapConcurrent;
































































function isParallelGroup(step) {
  return "parallel" in step && Array.isArray(step.parallel);
}

function isDynamicRunnerGroup(step) {
  return "expand" in step && "collect" in step && "parallel" in step && !Array.isArray(step.parallel);
}

function flattenSteps(steps) {
  const flat = [];
  for (const step of steps) {
    if (isParallelGroup(step)) {
      for (const task of step.parallel) flat.push(task);
    } else if (isDynamicRunnerGroup(step)) {
      continue;
    } else {
      flat.push(step);
    }
  }
  return flat;
}

const DEFAULT_GLOBAL_CONCURRENCY_LIMIT = exports.DEFAULT_GLOBAL_CONCURRENCY_LIMIT = 20;

/**
 * A promise-based semaphore for limiting concurrent access across multiple
 * mapConcurrent calls within a single run. Enforces a global cap on the total
 * number of subagent tasks executing simultaneously, regardless of each step's
 * per-step concurrency limit.
 */
class Semaphore {
  available;
  queue = [];

  constructor(limit) {
    this.available = Math.max(1, Math.floor(limit) || 1);
  }

  acquire() {
    if (this.available > 0) {
      this.available--;
      return Promise.resolve();
    }
    return new Promise((resolve) => {
      this.queue.push(resolve);
    });
  }

  release() {
    const next = this.queue.shift();
    if (next) {
      next();
    } else {
      this.available++;
    }
  }
}exports.Semaphore = Semaphore;

async function mapConcurrent(
items,
limit,
fn,
globalSemaphore)
{
  const safeLimit = Math.max(1, Math.floor(limit) || 1);
  const results = new Array(items.length);
  let next = 0;

  async function worker(_workerIndex) {
    while (next < items.length) {
      const i = next++;
      if (globalSemaphore) {
        await globalSemaphore.acquire();
        try {
          results[i] = await fn(items[i], i);
        } finally {
          globalSemaphore.release();
        }
      } else {
        results[i] = await fn(items[i], i);
      }
    }
  }

  await Promise.all(
    Array.from({ length: Math.min(safeLimit, items.length) }, (_, wi) => worker(wi))
  );
  return results;
}














function aggregateParallelOutputs(
results,
headerFormat = (i, agent) =>
`=== Parallel Task ${i + 1} (${agent}) ===`)
{
  return results.
  map((r, i) => {
    const header = headerFormat(r.taskIndex ?? i, r.agent);
    const hasOutput = Boolean(r.output?.trim());
    const status =
    r.timedOut ?
    `TIMED OUT${r.error ? `: ${r.error}` : ""}` :
    r.exitCode === -1 ?
    "SKIPPED" :
    r.exitCode !== 0 && r.exitCode !== null ?
    `FAILED (exit code ${r.exitCode})${r.error ? `: ${r.error}` : ""}` :
    r.error ?
    `WARNING: ${r.error}` :
    !hasOutput && r.outputTargetPath && r.outputTargetExists === false ?
    `EMPTY OUTPUT (expected output file missing: ${r.outputTargetPath})` :
    !hasOutput && !r.outputTargetPath ?
    "EMPTY OUTPUT (no textual response returned)" :
    "";
    const body = status ? hasOutput ? `${status}\n${r.output}` : status : r.output;
    return `${header}\n${body}`;
  }).
  join("\n\n");
}

const MAX_PARALLEL_CONCURRENCY = exports.MAX_PARALLEL_CONCURRENCY = 4; /* v9-83fd7e2ad1a0b9ef */
